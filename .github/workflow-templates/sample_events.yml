name: Git Action sample for Event Trigger

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  worlflow_dispatch: true

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Run a one-line script
        run: echo Hello from Event Trigger
